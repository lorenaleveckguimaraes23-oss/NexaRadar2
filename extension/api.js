const API_NEXA_RADAR="https://nexaradar2.onrender.com";
async function buscarProdutos(termo){const t=String(termo||"").trim();if(!t)throw new Error("Informe um produto para pesquisar.");const r=await fetch(`${API_NEXA_RADAR}/search?q=${encodeURIComponent(t)}`,{headers:{Accept:"application/json"}});if(!r.ok)throw new Error(`Erro do servidor: ${r.status}`);const d=await r.json();if(!d.sucesso)throw new Error(d.erro||"Não foi possível realizar a pesquisa.");return d.produtos||[];}
